
@{ 
    Layout = "~/Views/Shared/_BasicLayout.cshtml";
}

<div class="navbar navbar-expand-lg navbar-fixed-top mb-5 bg-dark navbar-dark">
        <div class="container">

            <div class="navbar-header">
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                @Html.ActionLink("Flores De Mayo", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>

            <div class="navbar-collapse collapse" id="navbar">
                <ul class="nav navbar-nav">
                    <li class="nav-item"> <a href="@Url.Action("CreateApplication", "Application", new { })" class="nav-link">Apply</a> </li>
                    <li class="nav-item"> <a href="@Url.Action("ViewApplicationStatus", "Application", new { })" class="nav-link">Application Status</a> </li>
                    <li class="nav-item"> <a href="@Url.Action("CreateAlayan", "Alayan", new { })" class="nav-link">Schedule an Alay</a> </li>
                    <li class="nav-item"> <a href="@Url.Action("AlayanSummary", "Alayan", new { })" class="nav-link">Alay Summary</a> </li>
                    <li class="nav-item"> <a href="@Url.Action("DisplayApplications", "Application", new { })" class="nav-link">Application</a> </li>
                    <li class="nav-item"> <a href="@Url.Action("Index", "Alayan", new { })" class="nav-link">Calendar</a> </li>
                    <li class="nav-item"> <a href="@Url.Action("DisplayAlays", "Alayan", new { })" class="nav-link">Alay List</a> </li>

                </ul>
                <div class="">
                    @Html.Partial("_LoginPartial")
                </div>
            </div>
        </div>
    </div>

    <div class="container">
        @RenderBody()
    </div>

    @RenderSection("scripts", required: false)
